# Copy to .env and fill in from your Supabase project settings (Project Settings -> API).
# Leave these blank to run the app fully offline: it falls back to on-device storage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
